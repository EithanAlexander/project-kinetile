/**
 * Brand images belong in `public/`:
 * - `Kinetile.jpeg` — favicon (`index.html`); keep filename in sync with that link.
 * - `project-kinetile.jpeg` — header and 404 page (`BRAND_MARK_URL`).
 */
export const BRAND_MARK_URL = '/project-kinetile.jpeg'
